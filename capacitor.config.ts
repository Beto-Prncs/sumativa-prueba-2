import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.camaraprinceso.app',
  appName: 'camaraprinceso',
  webDir: 'dist/appcamara/browser'
};

export default config;
